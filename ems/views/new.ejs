<!DOCTYPE html>
<html lang="en">
  <%- include('header') %>
  <body class="text-center">
    <%- include('navbar') %>
    <h1 class="text-5xl md:text-7xl font-bold text-center">Add Employee</h1>
    <br />
    <p class="text-2xl md:text-3xl font-light mb-5 md:mb-20 text-center">
      Add a new employee from this page.
    </p>

    <form method="post" action="/process">
      <div class="mb-2">
        <label for="firstName">First Name:</label>
        <input
          type="text"
          class="border py-1"
          id="firstName"
          name="firstName"
        />
      </div>
      <div>
        <label for="lastName">Last Name:</label>
        <input type="text" class="border py-1" id="lastName" name="lastName" />
      </div>

      <div>
        <!-- CSRF token -->
        <input name="_csrf" value="<%= csrfToken %>" type="hidden" />
        <br /><br />
        <button
          type="submit"
          class="bg-green-600 hover:bg-green-700 text-white font-bold py-2 px-4 rounded mb-96"
        >
          ADD
        </button>
      </div>
    </form>
  </body>
  <%- include('footer') %> <%- include('scripts') %>
</html>
