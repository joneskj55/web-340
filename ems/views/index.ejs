<!DOCTYPE html>
<html lang="en">
  <%- include('header') %>

  <body class="flex flex-col min-h-screen">
    <%- include('navbar') %>
    <main class="flex-grow mb-20">
      <h1 class="mt-6 text-5xl md:text-7xl font-bold text-center">
        Employee Management System
      </h1>
      <br />
      <p class="text-2xl md:text-3xl font-light mb-1 md:mb-20 text-center">
        Manage, enter, and view all employee information from this site.
      </p>
      <div class="container mx-auto px-6 pt-10">
        <div class="sm:flex sm:mt-8">
          <div
            class="mt-8 sm:mt-0 sm:w-full sm:px-8 flex flex-col md:flex-row justify-around"
          >
            <div class="flex flex-col">
              <span class="font-bold text-gray-700 mb-2 text-2xl"
                >Add a new employee</span
              >
              <span class="mb-2"
                ><p>
                  Add a new employee.<br />
                  Enter some basic information.<br />
                  Let us handle the hard work for you.
                </p></span
              >
              <span class="mb-10">
                <a href="/new">
                  <button
                    class="bg-green-700 hover:bg-green-800 text-white font-bold py-2 px-4 rounded"
                  >
                    Add
                  </button>
                </a>
              </span>
            </div>
            <div class="flex flex-col">
              <span class="font-bold text-gray-700 mb-2 text-2xl"
                >View a list of all employees</span
              >
              <span class="mb-2"
                ><p>
                  View a list of all current employees.<br />
                  Keep track of who you currently employ.<br />
                  See your full roster in one shot.
                </p></span
              >
              <span>
                <a href="/list">
                  <button
                    class="bg-green-700 hover:bg-green-800 text-white font-bold py-2 px-4 rounded"
                  >
                    List
                  </button>
                </a>
              </span>
            </div>
          </div>
        </div>
      </div>
    </main>
  </body>
  <%- include('footer') %> <%- include('scripts') %>
</html>
